const PostData = (res,redirect) => {
  if(res.profileObj.email){

    //alert("will check on database if " + res.profileObj.email  + " exist or not. If exist will forward to landing page else signup page" )
  
  }
    
    return true;
}
 
export default PostData;